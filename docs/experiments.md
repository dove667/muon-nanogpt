# Muon Newton-Schulz 正交化调度对比实验

## 一、研究问题

Muon 优化器通过 Newton-Schulz（NS）迭代将动量矩阵正交化。每次迭代由五次多项式系数 $(a, b, c)$ 定义：$p(\sigma) = a\sigma + b\sigma^3 + c\sigma^5$。不同系数决定了每次迭代将奇异值推向 1 的速度。

在 **T=5 步固定**的前提下，五种系数序列构成一个从慢到快的谱系：

| 策略 | 每步系数 | 收敛速度 |
|------|---------|----------|
| **Vanilla** | 5× $(2.0, -1.5, 0.5)$ | 最慢，标准 NS 迭代 |
| **Manual** | 3× 快速 + 2× 稳定 | 先快后慢 |
| **Fast** | 5× $(3.4445, -4.7750, 2.0315)$ | 最快，Keller Jordan 调优系数 |
| **Polar Express** | 每步动态计算 | 自适应 |
| **AdamW** | 无 | 基线 |

本实验在完全固定的训练栈上对比这五种策略。

## 二、控制变量

除 NS 系数序列外，**所有变量在各组间保持一致**。

### 硬件

| 参数 | 值 |
|------|-----|
| GPU | 4× RTX 4090（24GB VRAM） |
| CUDA | 12.1 |
| PyTorch | 2.5.1+cu121 |

### 训练栈

| 参数 | 值 |
|------|-----|
| 模型 | 11 层 GPT，hidden_dim=768，6 heads（head_dim=128），~162M 参数 |
| 数据 | FineWeb-10B（预分词、BOS 对齐分片） |
| 训练 token 预算 | **100M** |
| 验证间隔 | 每 10M tokens |
| 验证 token 数 | 2,097,152 |
| 精度 | BF16 |
| 序列长度 | 2048（全程固定） |
| 批次大小 | 固定，每步约 262K tokens（如 16×2048×8） |
| 梯度累积 | 32 步 |
| 随机种子 | $\{0, 1, 2\}$ |
| 注意力窗口 | 固定 short=3×128, long=7×128（取现有第二阶段配置） |
| LR 调度 | warmup（前 10% 步线性升至峰值）→ cosine decay 至峰值 10% |

### Muon 四组（Vanilla / Manual / Fast / PE）

| 参数 | 值 |
|------|-----|
| 矩阵参数学习率 | 0.023（`lr_mul=1.0`） |
| 动量 | 0.95 |
| NS 迭代步数 | **T=5** |
| 权重衰减 | 1.2 |

### 各策略系数序列

- **Vanilla**：5 步全用 $(2.0, -1.5, 0.5)$
- **Manual**：前 3 步 $(3.4445, -4.7750, 2.0315)$ + 后 2 步 $(2.0, -1.5, 0.5)$
- **Fast**：5 步全用 $(3.4445, -4.7750, 2.0315)$
- **Polar Express**：`T=5`，`lower_bound=1e-3`，`cushion=0.02`，`safety_factor=0.02`

### AdamW 组

| 参数 | 值 |
|------|-----|
| 学习率 | 0.008（与现有代码中 Adam 参数 LR 一致） |
| $\beta_1$, $\beta_2$ | (0.9, 0.95) |
| weight decay | 0.005 |

## 三、总运行量

5 配置 × 3 种子 = **15 轮**

每轮 100M tokens，总计 **1.5B tokens**。

## 四、评估指标

| 维度 | 指标 | 含义 |
|------|------|------|
| 最终效果 | `val/loss`（最终值，各种子间均值与标准差） | 验证损失 |
| 收敛速度 | `val/loss` 随 token 数变化曲线 | 同等预算下谁收敛更快 |
| 计算开销 | `train/step_time_ms`，正交化耗时占比 | 不同策略的墙钟时间 |
| 正交性质量 | `spec/update_orth_error` | $\|U^TU - I\|$ |
| 训练稳定性 | 各种子间 `val/loss` 标准差 | 不同种子下的波动 |

## 五、预期

- **Vanilla**（全慢）收敛最慢、正交质量可能不达标，但计算量最低
- **Fast**（全快）收敛最快，但可能"冲过头"导致正交精度差
- **Manual** 居于两者之间
- **PE** 期望在收敛速度与正交质量间取得最佳平衡，但有额外计算开销
- **AdamW** 作为无正交化的下界参照

## 六、实现说明

需要新增 `adamw` 和 `vanilla` 两种 orth 模式（现有代码中 "vanilla" 实际是 Fast），改动点：

1. **`orthogonalization.py`**：
   - 重命名现有 `"vanilla"` 分支为 `"fast"`
   - 新增 `"vanilla"` 分支（5 步全用 `STABLE_COEFF`）
   - 新增 `"adamw"` 分支
2. **`polar.py`**：`adamw` 模式返回恒等映射
3. **`optim/manager.py`**：`adamw` 模式下将矩阵参数改用 Adam 优化
